## Local environment variables (sanitized)
## WARNING: Do NOT commit secrets into the repository. Use environment variables
## or a secret manager for production. Replace the placeholders below on your
## local machine or set the real values in your deployment platform (Vercel,
## Supabase secrets, etc.).

# Public / client-side (anon) key - safe to keep as NEXT_PUBLIC / VITE var but do
# not commit real values.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=env(SUPABASE_ANON_KEY)

# Server-side keys (use only on server; never expose in client builds)
GEMINI_API_KEY=env(GEMINI_API_KEY)
API_URL=http://localhost:4000

# Backend DB connection - use service-side secret store, do not commit plaintext
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT.supabase.co:5432/postgres
SESSION_SECRET=env(SESSION_SECRET)

# Any blob or storage tokens should be kept secret
BLOB_READ_WRITE_TOKEN=env(BLOB_READ_WRITE_TOKEN)
SUPABASE_SECRET_KEY=env(SUPABASE_SECRET_KEY)

# Vite / frontend placeholders
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=env(VITE_SUPABASE_ANON_KEY)

